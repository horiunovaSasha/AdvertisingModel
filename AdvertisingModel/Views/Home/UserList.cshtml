@model IEnumerable<CustomUser>
 
<h2>Список користувачів</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Graph" asp-route-userid="@user.Id">Редагувати</a>
            </td>
        </tr>
    }
</table>