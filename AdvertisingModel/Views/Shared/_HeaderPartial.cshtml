@{
    bool isNotAdmin = !this.User.IsInRole("admin");
}


<header id="header" class="header">
    <div class="top-left">
        <div class="navbar-header">
            <a class="navbar-brand" href="./"><img src="~/images/logo.png" alt="Logo"></a>
            <a class="navbar-brand hidden" href="./"><img src="images/logo2.png" alt="Logo"></a>
            @*<a id="menuToggle" class="menutoggle"><i class="fa fa-bars"></i></a>*@
        </div>
    </div>
    <div class="top-right">
        <div class="header-menu">
            <div class="header-left">
                @if (!isNotAdmin)
                {
                <div class="dropdown">
                        <a href="/Home/Index" class="btn btn-secondary dropdown-toggle" style="color:darkgray">Моделі</a>
                </div>

                <div class="dropdown">
                    <a href="/Roles/Index" class="btn btn-secondary dropdown-toggle" style="color:darkgray">Ролі</a>
                </div>
                <div class="dropdown">
                    <a href="/Home/CreateUser" class="btn btn-secondary dropdown-toggle"  style="color:darkgray">Створити нового користувача</a>
                </div>

                <div class="dropdown">
                    <a href="/account/logout" class="btn btn-secondary dropdown-toggle" style="color:darkgray">Вийти</a>
                </div>
                }
            </div>
        </div>
    </div>
</header>